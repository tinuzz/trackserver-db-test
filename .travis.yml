language: php

branches:
  only:
  - master

matrix:
  include:
  - name: "MySQL 5.5 on Precise"
    dist: precise
    services:
    - mysql
  - name: "Trusty"
    dist: trusty
    addons:
      mariadb: '10.0'

before_script:
  - mysqladmin -h 127.0.0.1 -u root create trackserver

script: ./test.sh
